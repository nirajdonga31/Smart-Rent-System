# Testing Notes

- The backend package currently has no test script defined.
- For small route/controller changes, a lightweight module-load check is a useful minimum gate when full runtime tests are absent.
- Booking/auth changes deserve dedicated API or controller tests if the project adds a backend test harness later.
